# 安装耗时反馈

安装慢时，请提供下列信息，方便定位具体环节。安装前不需要填写，也不需要额外检查电脑。

- 使用的 Agent 名称，以及它运行的是本机终端还是云端终端（不清楚可写“不清楚”）。
- 从发送安装 prompt 到出现第一条安装命令，大约等待多久；期间是否等待用户授权。
- 安装器带有 `[秒数]` 的输出，或最后一条错误。分享前可遮住本机用户名和路径。
- 最终是文件下载完成、等待 Edge 加载扩展，还是已经进入智囊；界面显示哪个版本。

不需要提供登录信息、Cookie、浏览器配置目录或访问令牌。请勿通过删除浏览器全局缓存来排查。

## 验证记录的区分

| 验证 | 能确认的内容 |
| --- | --- |
| 安装器执行日志 | 版本查询、下载、校验、解压、写入文件分别耗时多少 |
| Windows PowerShell 5.1 CI | 中文与空格路径、首次安装、重复覆盖、旧文件清除、失败处理 |
| 真实 Edge 生命周期回归 | 旧版运行时启用新版、冷启动、重复入口、运行版本与偏好保留 |
| 无历史上下文的 prompt 盲读 | 指令是否明确要求首先执行安装命令；不代表实际安装或其他 Agent 平台测试 |
| 用户在 Treework 等平台的完整运行记录 | Agent 的前置步骤、授权等待和是否真正调用本机终端 |

一次本机快速安装不能替代不同 Agent 和不同用户电脑的验证。报告总耗时时，应分别记录执行命令之前的等待、安装器耗时，以及首次浏览器加载操作。
